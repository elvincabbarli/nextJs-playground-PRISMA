"use client";
import React from "react";

const AddToCard = () => {
  return (
    <div>
      <button onClick={() => console.log("first")}>Add To Card</button>
    </div>
  );
};

export default AddToCard;
